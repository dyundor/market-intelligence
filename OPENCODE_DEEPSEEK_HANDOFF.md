# Yundor Market Intelligence 交接文档

更新日期：2026-08-08  
交接对象：OpenCode + DeepSeek  
项目路径：`/Users/yundor/Downloads/windows 迁移过来的/站点搭建/market-intelligence`

## 1. 用户的持续开发目标

持续开发 Yundor Market Intelligence，直至形成可实际用于发现、联系、跟进并转化北美卫浴买家的完整销售情报闭环。

开发必须按 Sprint 小步增量进行，每个 Sprint 需要：

1. 检查当前状态和相关数据流。
2. 只做有明确业务价值的最小改动。
3. 执行构建、相关测试和实际数据验证。
4. 创建清晰的 Git 提交。
5. 报告文件、逻辑、验证、付费 API 用量、风险和下一步。
6. 完成后通知用户。

当前重中之重：增加“以销售产品为维度”的数据，交付真正可用的热卖产品列表，并尽量为产品提供可追溯的链接和图片。

所有开发都必须回答：

> 这个功能是否帮助 Yundor 更快找到真实客户？

优先级：

- P0：买家发现、买家排序、产品机会、可联系性、买家画像、开发策略、CRM 和转化跟进。
- P1：数据清洗、企业匹配、官网验证、贸易关系准确性、评分模型。
- P2：架构、性能和重构，只在直接支撑 P0/P1 时进行。

## 2. 接手前必读

按顺序完整阅读：

1. `AGENTS.md`
2. `PROJECT_STATE.md`
3. `PROJECT_DECISIONS.md`
4. `DATA_SOURCE_ROADMAP.md`
5. 本文档

注意：`PROJECT_STATE.md` 和 `PROJECT_DECISIONS.md` 当前在工作区中是未跟踪文件，`AGENTS.md` 也有用户未提交的修改。不得删除、覆盖或顺手提交这些用户文件。它们的 Sprint 记录没有完全追踪到最新的 15.68，当前代码和 Git 历史才是最新实施证据。

## 3. 当前真实状态

当前分支：`main`  
最新提交：`472f12c Sprint 15.68: Link hot products to buyers and sources`

最近关键 Sprint：

- `472f12c` Sprint 15.68：热卖产品关联官方代表产品页、可追溯图片和买家直达。
- `d5f8135` Sprint 15.67：基于已存提单新增热卖产品榜。
- `91f2e07` Sprint 15.66：继续扩大已验证买家官网覆盖。
- `fa39813` Sprint 15.65：审核 World and Main 当前官网状态，防止死链接/恶意重定向被当成官网。
- `6ddf104` Sprint 15.64：官网候选评分和 VETTA/Vesta 同名企业排除。
- `d24c685` Sprint 15.63：官网研究流程和关联企业气泡。
- `642a18c` Sprint 15.61：修复企业详情打开时 `setCompanyDetailMonth is not defined`。
- `c21c453` Sprint 15.59：修正 HS 相关度虚假 `0%` 展示，缺少 HS 数据时不再伪装成不匹配。

最后一次完整验证：

- `npm test`：155 项测试全部通过。
- 生产构建通过。
- 本地 `GET /api/hot-products`：HTTP 200。
- 本地热卖榜数据基础：195 条已存美国海运提单。
- ImportYeti 付费额度消耗：0 credits。

## 4. 已实现的销售情报能力

系统已具备的主要能力：

- 从贸易数据发现美国买家和海外供应商。
- 企业标准化、别名、同名主体排除和关联企业标记。
- 买家技术资格、商业适配、联系价值和销售优先级。
- OEM/ODM、自有品牌、分销合作等开发策略。
- 线索清单、联系路由、外联草稿、跟进任务、机会金额、报价准备和结果反馈。
- 严格的联系人证据要求：不推测邮箱，不把未验证电话/邮箱当成可直接联系路由。
- 官网专项：买家优先队列、截断企业名修复、搜索组合、候选评分、多证据校验、安全的“无活跃官网”状态。
- 产品维度榜单：从噪声提单描述归一化为销售产品，聚合近 12 月提单、买家数、重量、最新交易和主要买家。
- 热卖产品支持点击主要买家进入企业详情。

## 5. 热卖产品榜当前实情

相关文件：

- `lib/products/hot-products.ts`
- `app/api/hot-products/route.ts`
- `app/page.tsx` 中的 `HotProductList`
- `tests/shipments.test.ts`

当前归一化产品：

- 浴室水龙头
- 龙头配件与零件
- 淋浴底盆
- 浴缸
- 淋浴房与淋浴门
- 花洒与淋浴系统
- 淋浴背板与墙板
- 浴缸与淋浴排水件
- 卫浴阀门

当前相对热度前四（基于当前 195 条样本）：

1. 淋浴底盆：98
2. 浴缸：66
3. 淋浴房与淋浴门：64
4. 浴室水龙头：63

相对热度公式：

- 近 12 月提单相对值：55%
- 买家覆盖相对值：30%
- 重量对数相对值：15%

必须正确描述：这是“当前已存提单样本中的相对热度”，不是美国整体市场销量或零售销量。

已验证的代表产品资源：

- DreamLine SlimLine 淋浴底盆官方产品页，图片地址尚未稳定取得。
- American Standard Studio 浴缸官方产品页和官方 CDN 图片。
- DreamLine Unidoor 淋浴门官方集合页和官方 Cloudinary 图片。
- Delta Lahara 浴室龙头官方产品页，图片地址尚未稳定取得。

图片必须标记为“代表产品”，不能声称是某票提单中的具体 SKU。只能使用能追溯到品牌方或可靠零售商产品页的图片。

## 6. 当前最大未完成问题

### 6.1 产品榜覆盖仍然太小

只有 195 条已存提单，不足以支撑全市场结论。应优先扩大免费、合法、可追溯的企业级提单覆盖，而不是继续调整分数让结果看起来更精确。

### 6.2 产品榜还没有完全接入销售资格

当前 `topBuyers` 是基于提单聚合的企业。其中可能包含：

- 已经被判定为同行制造商的企业。
- 已停业或已被收购的主体。
- 身份仍是模糊候选的企业。
- 产品有交易证据，但不适合 Yundor 供应链的主体。

下一步必须在“产品买家列表”中同时展示商业资格、身份状态、联系准备度和排除原因，不得把所有提单收货方都当成可开发买家。

### 6.3 提单产品描述有噪声和混装

一条提单可能同时提到底盆、背板、淋浴门和配件。当前允许一条混装提单支持多个产品，因此各产品的提单数不能相加得到总提单数。前端已有提示，后续不得删除。

### 6.4 代表产品图片尚未覆盖所有品类

当前 9 个品类中只有 4 个官方代表产品页，其中只有 2 个有稳定、可追溯的图片 URL。

### 6.5 官网补全是持续任务，不是单个企业补录

不要为 World and Main、VETTA 或其他单个企业写一次性特例。应持续改进通用流程：

> 待研究队列 → 搜索名称修复 → 候选搜索 → 身份评分 → 独立证据验证 → 安全入库 → 关联企业展示/待复核

## 7. 推荐的下一个 Sprint

### Sprint 15.69：产品→可开发买家链路

业务目标：让销售看到热卖产品后，能立即找到值得联系的真实买家，而不是仅看一个品类热度数字。

推荐实施：

1. 在 `rankHotProducts` 的买家聚合基础上，由 API 关联企业身份、线索、联系人、商业适配和销售阶段。
2. 新增产品买家明细或抽屉，至少展示：
   - 企业名
   - 该产品提单数
   - 最新该产品交易日期
   - 商业适配或销售优先级
   - 身份验证状态
   - 联系准备度
   - 已排除/不宜开发原因
3. 默认优先排列“身份可靠 + 商业匹配 + 有可验证联系路由”的买家。
4. 不删除被排除主体的原始数据；仅标记为不建议开发并保留原因。
5. 为聚合、排序、排除状态和数据保留添加定向测试。

验收标准：

- 点击一个热卖产品可看到全部相关买家，不只是 Top 3。
- 销售可以分清“有交易证据”和“值得开发”的差异。
- 点击合格买家可进入现有企业详情/CRM 链路。
- 被排除的同行制造商、模糊身份和停业主体不得被显示为优先销售线索。
- 不调用付费 API。

## 8. 后续产品维度 Sprint 候选

### Sprint 15.70：代表产品资源覆盖

- 将官方产品页扩大到全部 9 个归一化品类。
- 为每条资源保留品牌、型号、产品 URL、图片 URL、来源类型和核验日期。
- 图片加载失败必须有占位状态，不得影响榜单使用。
- 不使用不明搜索缩略图、AI 生成图或无法追溯来源的热链图片代表市场产品。

### Sprint 15.71：产品趋势和数据可信度

- 按月展示各产品提单数、买家数和重量趋势。
- 明确展示样本期间、提单数、已识别比例、混装比例和未分类比例。
- 排名同时展示置信度，样本太小时不输出强结论。
- 当扩大数据覆盖后，保持现有原始提单和聚合可重建性。

## 9. 数据与付费 API 底线

ImportYeti 总预算：100 credits。  
默认最低保留：25 credits。  
当前已知消耗：0 credits。

任何 ImportYeti 请求前，必须：

1. 查持久缓存。
2. 查免费数据源能否回答。
3. 估算 credits、占原始 100 credits 比例、占当前余额比例。
4. 向用户展示估算。
5. 获得用户明确审批。
6. 未获得审批绝对不能调用。
7. 实际费用可能超过已批准上限时，必须再次停止并申请。

测试只能使用 mock/fixture，不得调用付费 API。所有付费请求必须经过现有 gateway，不得从 UI 或新建快捷接口绕过。

数据保护：

- NULL 永远不得覆盖现有非空值。
- 简略地址不得覆盖完整地址。
- 缺少官网的新数据不得删除旧官网。
- 较低质量数据不得覆盖更丰富的 enrichment 数据。
- 不通过把值清空来“纠错”；改为标记 unverified 并保留原始值与证据。
- 保留原始外部数据，使分类、聚合和排名可重建。

## 10. 本地开发与验证

安装依赖后常用命令：

```bash
npm run dev
npm run build
npm test
npm run lint
```

当前 `npm test` 会先运行生产构建，然后运行 8 个定向测试文件。最后一次是 155 项测试全通过。

本地 D1 数据库通常位于：

```text
.wrangler/state/v3/d1/miniflare-D1DatabaseObject/*.sqlite
```

可能有多个 SQLite 文件。现有研究脚本通常选择该目录下最近修改且非 `metadata.sqlite` 的文件。不要假设固定文件名。

官网任务：

```bash
npm run websites:queue
npm run websites:import -- data/public-company-websites-waveN-YYYY-MM-DD.json
```

销售初始化脚本：

```bash
npm run sales:initialize
npm run sales:drafts
npm run sales:tasks
```

执行任何写入脚本前，先阅读脚本、确认数据库目标、检查是否有 dry-run/事务保护和数据保留逻辑。

## 11. Git 与工作区注意事项

交接时已存在的用户改动：

```text
 M AGENTS.md
?? PROJECT_DECISIONS.md
?? PROJECT_STATE.md
```

这些不是本轮交接文档工作的改动。继续开发时必须：

- 不覆盖用户改动。
- 不用 `git reset --hard` 或其他破坏性操作。
- 每个 Sprint 只暂存明确相关文件。
- 提交信息使用 `Sprint XX.XX: Short description`。
- 提交后报告 commit hash。

## 12. 不要做的事

- 不要继续扩展 `/Users/yundor/yundor-agent-bridge`；v0.2 能力已满足需求。
- 不要为了技术复杂度而开发。
- 不要重写已经可用的 Query Engine、provider、cache、gateway 或 CRM 模块。
- 不要把聚合贸易统计宣称为企业级提单。
- 不要把搜索结果页、目录站或同名企业当成已验证官网。
- 不要因为搜到一个同名结果就直接写入数据库。
- 不要把代表产品图片说成提单中的具体 SKU。
- 不要伪造联系人、邮箱、电话、产品图片、销量或数据覆盖。
- 不要在测试中调用付费 API。

## 13. 每个 Sprint 完成报告格式

```markdown
## Completed

## Files Changed

## Technical Changes

## Validation

## Credit Usage

## Risks

## Next Step
```

用户要的不是“看起来很完整的系统”，而是销售能够靠它更快找到真实北美卫浴买家、找到可验证的联系途径、记录跟进并推进成交。
